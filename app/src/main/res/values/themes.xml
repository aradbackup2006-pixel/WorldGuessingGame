<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WorldGuessingGame" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="LetterButton">
        <item name="android:background">@drawable/btn_style</item>
        <item name="android:textColor">#ffff</item>
        <item name="android:textSize">18sp</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:minWidth">48dp</item>
        <item name="android:gravity">center</item>

    </style>
    <style name="Theme.WorldGuessingGame" parent="Base.Theme.WorldGuessingGame" />
</resources>